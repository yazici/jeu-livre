using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Moleculator : MonoBehaviour
{

    public Transform panel1;
    public Transform panel2;

    // Start is called before the first frame update
    void Start()
    {



    }

    // Update is called once per frame
    void Update()
    {
        //panel2.SetSiblingIndex(0);
        //Debug.Log("panel1 index : " + panel1.GetSiblingIndex());
       // Debug.Log("panel2 index : " + panel2.GetSiblingIndex());

    }
}
